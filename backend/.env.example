PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017
DB_NAME=portfolio
# Comma-separated (optional). Leave empty to allow any origin.
CORS_ORIGIN=http://localhost:5500,http://127.0.0.1:5500
# Optional. If true, the backend also serves the frontend `index.html` from the repo root.
SERVE_FRONTEND=false
